const add = (a, b) => {
    return a + b;
};

module.exports = add;

// para llamarlo en node el codigo es:
// const add = require('./add');

// para el resultado escribe en node
// multiply(2, add(4, 4));